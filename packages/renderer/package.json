{
  "name": "@app/renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vite build && vitest run"
  },
  "devDependencies": {
    "@app/shared": "*",
    "typescript": "^5.4.5",
    "vite": "^5.2.0",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.9.0"
  }
}
